# v7.3 P2 — Med/High Align (Spec-Lock)

Label: **P2 of P1–P3**. Low Freeze immutable (P1). No P3 FX/LOD.

## High differentiation
- `maxPixelRatio = Math.min(2, dpr)`
- `shadowMapSize = 2048` when `width >= 1280 && dpr >= 2`, else `1024`
- `fxScale = 1.15`
- `petalCap / mistCap / sparkleCap = 56 / 32 / 56`
- `exposure = 1.30`
- `sparkleBurst = 3` (freeze)
- Non-low shadow: radius `2.8`, far `90` (IST)

## Med
- Unchanged freeze (optional Shadow-1024 OUT of P2).

## A11y reduced-motion
- petalCap already `Math.min(8, …)`
- mistCap / sparkleCap consumers: `Math.min(8, …)`
- sparkleBurst: `Math.min(1, …)`

## Ops
- Desktop High Thermik: Shadow 2048 / fxScale 1.15 / caps 56 — not a merge blocker.
