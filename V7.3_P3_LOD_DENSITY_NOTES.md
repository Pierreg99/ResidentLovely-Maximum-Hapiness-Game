# v7.3 P3 — LOD-Density only (P3 of P1–P3)

## Contract
- Scope: `src/world/scene.js` Caps/LOD consumers + `src/main.js` anchor wire.
- Low / Med / High preset ceilings Freeze (P1/P2). Caps only decrease via LOD.
- New-FX OUT. Pack-Extras OUT. sparkleBurst Freeze (no LOD). A11y ≤8 / burst ≤1 unchanged.

## LOD table (Camera→Player, world units)
| Tier | Distance | Mult on petal/mist/sparkleCap |
|------|----------|-------------------------------|
| near | `< 12`   | 1.0 |
| mid  | `12–28`  | 0.7 |
| far  | `> 28`   | 0.45 |

Result: `Math.floor(cap * mult)`, min 1 if cap>0 else 0.

## Order
Cap-Ceiling (preset + A11y) → LOD mult → fxScale (existing callers).

## Apply sites
- Petals: pool at ceiling; active count from LOD at `initPetals`; retier only on tier change via `updateLodAnchor`.
- Mist: LOD at spawn in `updateGroundMist`.
- Sparkle pool: LOD at `spawnSparkleFootstep`; burst unchanged.

## Thermik (Desktop)
Far-tier density cut (0.45) is intentional for Desktop High Thermik relief when Camera→Player distance > 28. Near play stays full ceiling. Ops residual: watch GPU Thermik on High after Billing/CI green; no Med/High preset retune in P3.

## AC
Suite ≥257; Label P3 of P1–P3; Critic/Ethicist Re-Gate before merge.
